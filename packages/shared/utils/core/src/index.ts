/**
 * `@foundation/core` — helpers shared across the foundation toolchain (qa, sync,
 * and the record validator): repo-root resolution and the `bin/mise` wrapper.
 *
 * @module
 */

export { repoRoot } from "./repo-root.ts";
export { miseExec } from "./mise.ts";
