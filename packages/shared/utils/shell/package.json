{
	"$schema": "../../../../.schemas/package.json",
	"name": "@foundation/shell",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"description": "Foundation shell primitives: execa-backed sh() with pipefail equivalents, enforced timeouts, closed stdin, structured errors, and a journal for streaming child output.",
	"license": "SEE LICENSE IN LICENSE",
	"dependencies": {
		"execa": "9.6.1"
	},
	"devDependencies": {
		"@foundation/config": "workspace:*"
	},
	"exports": {
		".": "./src/index.ts"
	}
}
