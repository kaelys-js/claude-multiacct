{
  "$schema": "https://raw.githubusercontent.com/kaelys-js/kaelys-js/main/schema/kaelys-catalog.schema.json",
  "name": "claude-multiacct",
  "tagline": "Sign in to more than one Claude Desktop account on the same Mac, each in its own Dock-clickable app.",
  "sellMe": "Claude Desktop only signs in to one Anthropic account at a time — switching means signing out and back in, and losing session history in between. This CLI clones the app once per account, wires each clone to its own OAuth directory (`--user-data-dir` + `CLAUDE_CONFIG_DIR`), and rewrites just enough of the bundle so the Dock lights the right icon for the right window. Sessions and agent metadata are shared safely across clones via symlinks (JSONL, UI, agent) and rsync (Chromium storage), so history follows you between accounts. Ships with three launchd agents that keep clones fresh when Anthropic pushes an update.",
  "status": "stable",
  "since": 2026,
  "primaryLanguage": "Shell",
  "technology": [
    "Bash",
    "bats",
    "launchd",
    "macos",
    "mise"
  ],
  "topics": [
    "bash",
    "claude",
    "claude-desktop",
    "electron",
    "launchd",
    "macos",
    "multi-account",
    "oauth"
  ],
  "products": [
    {
      "name": "claude-multiacct",
      "description": "CLI for installing account instances, sharing sessions between them, repairing drifted clones, and following the sync log."
    }
  ],
  "links": {
    "homepage": "https://github.com/kaelys-js/claude-multiacct",
    "issues": "https://github.com/kaelys-js/claude-multiacct/issues",
    "discussions": "https://github.com/kaelys-js/claude-multiacct/discussions"
  }
}
