# yaml-language-server: $schema=../../.schemas/github-workflow.json
# CI gate for docs-only changes. ci.yml carries `paths-ignore` for the same
# list; when a push or PR touches ONLY files in that list, ci.yml skips and
# GitHub leaves the required checks (`format`, `lint`, `sync`, `test`) in
# Pending — branch protection then blocks the docs-only PR from merging.
# This workflow mirrors the path list under `paths:` (not `paths-ignore:`)
# and matches the required-check names via `jobs.<id>.name`.
#
# `format` and `lint` are NOT stubs: they run the exact same steps as ci.yml's
# format/lint jobs, so a docs-only change with unformatted or lint-dirty
# markdown genuinely fails the gate instead of merging clean and surfacing
# later on the next code PR. `sync` and `test` stay fast echo stubs — neither
# meaningfully applies to a docs-only change and both are heavy.
#
# Keep the path list here in sync with ci.yml.
name: CI

on:
  push:
    branches: [main]
    paths:
      - "docs/**"
      - "*.md"
      - "LICENSE"
      - ".editorconfig"
      - ".gitattributes"
      - ".github/CODEOWNERS"
      - ".github/copilot-instructions.md"
      - ".github/ISSUE_TEMPLATE/**"
      - ".github/PULL_REQUEST_TEMPLATE.md"
      - "SUPPORT.md"
      - "CODE_OF_CONDUCT.md"
      - "CONTRIBUTING.md"
      - "SECURITY.md"
  pull_request:
    branches: [main]
    paths:
      - "docs/**"
      - "*.md"
      - "LICENSE"
      - ".editorconfig"
      - ".gitattributes"
      - ".github/CODEOWNERS"
      - ".github/copilot-instructions.md"
      - ".github/ISSUE_TEMPLATE/**"
      - ".github/PULL_REQUEST_TEMPLATE.md"
      - "SUPPORT.md"
      - "CODE_OF_CONDUCT.md"
      - "CONTRIBUTING.md"
      - "SECURITY.md"

concurrency:
  group: ci-noop-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions:
  contents: read

env:
  CI: "true" # pnpm 11: allow non-interactive node_modules rebuild

jobs:
  format:
    name: format
    # Route to the same self-hosted runner as ci.yml so runs are billed against
    # the runner (free) rather than the paid Actions plan. Falls back to
    # ubuntu-latest if the mini is down or the escape hatch is flipped off.
    runs-on: ${{ vars.SELF_HOSTED_ENABLED == 'true' && fromJSON('["self-hosted","kaelys-mac-mini"]') || 'ubuntu-latest' }}
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0
      - name: Drop host .mise (act only, see ci.yml header)
        if: env.ACT == 'true'
        run: rm -rf .mise
      - name: Restore mise toolchain
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: .mise
          key: mise-${{ runner.os }}-${{ hashFiles('mise.toml', 'mise.lock') }}
      - name: Expose vendored mise on PATH
        run: |
          ./bin/mise --version >/dev/null 2>&1 || true
          mkdir -p "$HOME/.local/bin"
          ln -sf "$(ls "$PWD"/.mise/mise-* | head -n1)" "$HOME/.local/bin/mise"
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"
      - name: Install toolchain (mise — node, pnpm, all formatters)
        run: ./bin/mise install
      - name: Install dependencies
        run: ./bin/mise exec -- pnpm install --frozen-lockfile
      - name: Format check
        run: ./bin/mise exec -- pnpm run qa:format:check
  lint:
    name: lint
    runs-on: ${{ vars.SELF_HOSTED_ENABLED == 'true' && fromJSON('["self-hosted","kaelys-mac-mini"]') || 'ubuntu-latest' }}
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0
      - name: Drop host .mise (act only, see ci.yml header)
        if: env.ACT == 'true'
        run: rm -rf .mise
      - name: Restore mise toolchain
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: .mise
          key: mise-${{ runner.os }}-${{ hashFiles('mise.toml', 'mise.lock') }}
      - name: Expose vendored mise on PATH
        run: |
          ./bin/mise --version >/dev/null 2>&1 || true
          mkdir -p "$HOME/.local/bin"
          ln -sf "$(ls "$PWD"/.mise/mise-* | head -n1)" "$HOME/.local/bin/mise"
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"
      - name: Install toolchain (mise — node, pnpm, all linters)
        run: ./bin/mise install
      - name: Install dependencies
        run: ./bin/mise exec -- pnpm install --frozen-lockfile
      - name: Lint (strict — zero diagnostics required)
        run: ./bin/mise exec -- pnpm run qa:lint
  sync:
    name: sync
    runs-on: ${{ vars.SELF_HOSTED_ENABLED == 'true' && fromJSON('["self-hosted","kaelys-mac-mini"]') || 'ubuntu-latest' }}
    timeout-minutes: 2
    steps:
      - run: echo "docs-only change - sync gate does not apply (see ci.yml paths-ignore)"
  test:
    name: test
    runs-on: ${{ vars.SELF_HOSTED_ENABLED == 'true' && fromJSON('["self-hosted","kaelys-mac-mini"]') || 'ubuntu-latest' }}
    timeout-minutes: 2
    steps:
      - run: echo "docs-only change - test gate does not apply (see ci.yml paths-ignore)"
  gates:
    name: gates
    runs-on: ${{ vars.SELF_HOSTED_ENABLED == 'true' && fromJSON('["self-hosted","kaelys-mac-mini"]') || 'ubuntu-latest' }}
    timeout-minutes: 2
    steps:
      - run: echo "docs-only change - gates gate does not apply (see ci.yml paths-ignore)"
