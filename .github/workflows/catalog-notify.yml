# yaml-language-server: $schema=../../.schemas/github-workflow.json
# Notifies kaelys-js/kaelys-js when this repo's .github/kaelys-catalog.json
# changes on main. The sync-catalog workflow over there listens for the
# `catalog-updated` repository_dispatch event and re-renders the profile
# README. See docs/catalog-setup.md in kaelys-js/kaelys-js for the one-time
# GitHub App setup that provisions the two secrets used here.
name: catalog-notify

"on":
  push:
    branches: [main]
    paths:
      - ".github/kaelys-catalog.json"
  workflow_dispatch:
    inputs:
      dry-run:
        description: "Report what would be dispatched to kaelys-js/kaelys-js without sending the repository_dispatch event."
        type: boolean
        default: false

concurrency:
  group: catalog-notify
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  preflight:
    name: preflight
    # Hosted-first runner selection. Query the account's GitHub Actions billing
    # usage for the current month and prefer GitHub-hosted runners while
    # included minutes remain: the post-discount netAmount summed over the
    # `actions` product stays 0 until the free minutes are used up, then goes
    # positive once overage is billed. While it is 0, runs_on is "ubuntu-latest"
    # (hosted-first); once minutes are exhausted it flips to the free
    # kaelys-mac-mini self-hosted runner. Any billing error fails safe to
    # self-hosted so CI never blocks on the billing API. hosted_ok exposes the
    # same decision as a boolean for jobs whose hosted runner is not
    # ubuntu-latest (e.g. macOS). See docs/self-hosted-runner.md in
    # kaelys-js-infra for the fleet-wide rationale.
    runs-on: ubuntu-latest
    timeout-minutes: 2
    outputs:
      runs_on: ${{ steps.pick.outputs.runs_on }}
      hosted_ok: ${{ steps.pick.outputs.hosted_ok }}
    steps:
      - name: Select runner by remaining GitHub-hosted minutes
        id: pick
        env:
          BILLING_READ_TOKEN: ${{ secrets.BILLING_READ_TOKEN }}
          IS_FORK: ${{ github.event.pull_request.head.repo.fork }}
        run: |
          set -uo pipefail
          SELF_HOSTED='["self-hosted","kaelys-mac-mini"]'
          HOSTED='"ubuntu-latest"'
          emit() {
            {
              echo "runs_on=$1"
              echo "hosted_ok=$2"
            } >> "$GITHUB_OUTPUT"
            echo "selected runner: $1 (hosted_ok=$2)"
          }
          # Fork PRs cannot use self-hosted runners (GitHub refuses to run fork
          # code on them) and never receive repo secrets, so GitHub-hosted
          # ubuntu is both the only option and the secure one. Short-circuit to
          # hosted before the billing call — also covers a missing token.
          if [ "${IS_FORK:-}" = 'true' ] || [ -z "${BILLING_READ_TOKEN:-}" ]; then
            echo 'fork PR or no billing token; using GitHub-hosted ubuntu-latest'
            emit "$HOSTED" true
            exit 0
          fi
          year="$(date -u +%Y)"
          month="$(date -u +%-m)"
          resp="$(curl -sS -m 30 -w '\n%{http_code}' \
            -H 'Accept: application/vnd.github+json' \
            -H "Authorization: Bearer ${BILLING_READ_TOKEN}" \
            -H 'X-GitHub-Api-Version: 2022-11-28' \
            "https://api.github.com/users/kaelys-js/settings/billing/usage?year=${year}&month=${month}")" \
            || { echo 'billing request failed; failing safe to self-hosted'; emit "$SELF_HOSTED" false; exit 0; }
          code="$(printf '%s' "$resp" | tail -n1)"
          body="$(printf '%s' "$resp" | sed '$d')"
          if [ "$code" != '200' ]; then
            echo "billing HTTP ${code}; failing safe to self-hosted"
            emit "$SELF_HOSTED" false
            exit 0
          fi
          if ! printf '%s' "$body" | jq -e '.usageItems | type == "array"' >/dev/null 2>&1; then
            echo 'billing response has no usageItems[]; failing safe to self-hosted'
            emit "$SELF_HOSTED" false
            exit 0
          fi
          net="$(printf '%s' "$body" | jq -r '[.usageItems[] | select(.product == "actions") | (.netAmount // 0)] | add // 0')"
          if ! printf '%s' "$net" | grep -Eq '^-?[0-9]+([.][0-9]+)?$'; then
            echo "unparsable actions netAmount '${net}'; failing safe to self-hosted"
            emit "$SELF_HOSTED" false
            exit 0
          fi
          if awk -v n="$net" 'BEGIN { exit !(n + 0 > 0) }'; then
            echo "actions netAmount=${net} > 0 — included minutes exhausted; using self-hosted"
            emit "$SELF_HOSTED" false
          else
            echo "actions netAmount=${net} == 0 — within included minutes; using hosted"
            emit "$HOSTED" true
          fi
  notify:
    name: dispatch catalog-updated
    needs: preflight
    runs-on: ${{ fromJSON(needs.preflight.outputs.runs_on) }}
    timeout-minutes: 5
    steps:
      - name: Verify GitHub App secrets are configured
        env:
          APP_CID: ${{ secrets.KAELYS_CATALOG_APP_CLIENT_ID }}
          APP_PK: ${{ secrets.KAELYS_CATALOG_APP_PRIVATE_KEY }}
        run: |
          if [[ -z "$APP_CID" || -z "$APP_PK" ]]; then
            printf 'Catalog notify is not configured yet.\n'
            printf '\n'
            printf 'One-time setup: see docs/catalog-setup.md in kaelys-js/kaelys-js\n'
            printf '\n'
            printf 'Missing secrets:\n'
            [[ -z "$APP_CID" ]] && printf '  - KAELYS_CATALOG_APP_CLIENT_ID\n'
            [[ -z "$APP_PK" ]] && printf '  - KAELYS_CATALOG_APP_PRIVATE_KEY\n'
            exit 1
          fi

      - name: Mint GitHub App installation token
        id: app-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          client-id: ${{ secrets.KAELYS_CATALOG_APP_CLIENT_ID }}
          private-key: ${{ secrets.KAELYS_CATALOG_APP_PRIVATE_KEY }}
          # Cross-repo dispatch: the token needs write access on the TARGET
          # repo (kaelys-js/kaelys-js). Restrict the token to just that repo.
          owner: ${{ github.repository_owner }}
          repositories: kaelys-js

      - name: Send repository_dispatch to kaelys-js/kaelys-js
        env:
          GITHUB_TOKEN: ${{ steps.app-token.outputs.token }}
          SRC_REPO: ${{ github.repository }}
          SRC_SHA: ${{ github.sha }}
        run: |
          if [ "${{ inputs.dry-run }}" = "true" ]; then
            echo "dry-run: would POST repos/${GITHUB_REPOSITORY_OWNER}/kaelys-js/dispatches"
            echo "dry-run:   event_type=catalog-updated"
            echo "dry-run:   client_payload[repo]=${SRC_REPO}"
            echo "dry-run:   client_payload[sha]=${SRC_SHA}"
            exit 0
          fi
          gh api "repos/${GITHUB_REPOSITORY_OWNER}/kaelys-js/dispatches" \
            --method POST \
            --field event_type=catalog-updated \
            --raw-field "client_payload[repo]=${SRC_REPO}" \
            --raw-field "client_payload[sha]=${SRC_SHA}"
