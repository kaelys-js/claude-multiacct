# yaml-language-server: $schema=../../.schemas/github-workflow.json
# Notifies kaelys-js/kaelys-js when this repo's .github/kaelys-catalog.json
# changes on main. The sync-catalog workflow over there listens for the
# `catalog-updated` repository_dispatch event and re-renders the profile
# README. See docs/catalog-setup.md in kaelys-js/kaelys-js for the one-time
# GitHub App setup that provisions the two secrets used here.
name: catalog-notify

"on":
  push:
    branches: [main]
    paths:
      - ".github/kaelys-catalog.json"
  workflow_dispatch:
    inputs:
      dry-run:
        description: "Report what would be dispatched to kaelys-js/kaelys-js without sending the repository_dispatch event."
        type: boolean
        default: false

concurrency:
  group: catalog-notify
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  notify:
    name: dispatch catalog-updated
    # Route to the kaelys-mac-mini self-hosted runner when SELF_HOSTED_ENABLED
    # is set on the repo; fall back to ubuntu-latest if the mini is down or
    # the escape hatch is flipped off.
    runs-on: ${{ vars.SELF_HOSTED_ENABLED == 'true' && fromJSON('["self-hosted","kaelys-mac-mini"]') || 'ubuntu-latest' }}
    timeout-minutes: 5
    steps:
      - name: Verify GitHub App secrets are configured
        env:
          APP_CID: ${{ secrets.KAELYS_CATALOG_APP_CLIENT_ID }}
          APP_PK: ${{ secrets.KAELYS_CATALOG_APP_PRIVATE_KEY }}
        run: |
          if [[ -z "$APP_CID" || -z "$APP_PK" ]]; then
            printf 'Catalog notify is not configured yet.\n'
            printf '\n'
            printf 'One-time setup: see docs/catalog-setup.md in kaelys-js/kaelys-js\n'
            printf '\n'
            printf 'Missing secrets:\n'
            [[ -z "$APP_CID" ]] && printf '  - KAELYS_CATALOG_APP_CLIENT_ID\n'
            [[ -z "$APP_PK" ]] && printf '  - KAELYS_CATALOG_APP_PRIVATE_KEY\n'
            exit 1
          fi

      - name: Mint GitHub App installation token
        id: app-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          client-id: ${{ secrets.KAELYS_CATALOG_APP_CLIENT_ID }}
          private-key: ${{ secrets.KAELYS_CATALOG_APP_PRIVATE_KEY }}
          # Cross-repo dispatch: the token needs write access on the TARGET
          # repo (kaelys-js/kaelys-js). Restrict the token to just that repo.
          owner: ${{ github.repository_owner }}
          repositories: kaelys-js

      - name: Send repository_dispatch to kaelys-js/kaelys-js
        env:
          GITHUB_TOKEN: ${{ steps.app-token.outputs.token }}
          SRC_REPO: ${{ github.repository }}
          SRC_SHA: ${{ github.sha }}
        run: |
          if [ "${{ inputs.dry-run }}" = "true" ]; then
            echo "dry-run: would POST repos/${GITHUB_REPOSITORY_OWNER}/kaelys-js/dispatches"
            echo "dry-run:   event_type=catalog-updated"
            echo "dry-run:   client_payload[repo]=${SRC_REPO}"
            echo "dry-run:   client_payload[sha]=${SRC_SHA}"
            exit 0
          fi
          gh api "repos/${GITHUB_REPOSITORY_OWNER}/kaelys-js/dispatches" \
            --method POST \
            --field event_type=catalog-updated \
            --raw-field "client_payload[repo]=${SRC_REPO}" \
            --raw-field "client_payload[sha]=${SRC_SHA}"
